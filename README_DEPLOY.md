# 🚀 Deploy SPU Activity Hub - วิธีง่ายที่สุด!

## ⚡ วิธีที่ 1: Docker (แนะนำ - ง่ายที่สุด!)

### แค่ 3 คำสั่ง!

```bash
# 1. ติดตั้ง Docker
curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh

# 2. อัปโหลดไฟล์ (จากเครื่อง local)
scp -r * root@YOUR_VPS_IP:/root/spu-activity-hub/

# 3. Deploy!
ssh root@YOUR_VPS_IP
cd /root/spu-activity-hub
docker compose up -d
```

**เสร็จแล้ว!** เปิด `http://YOUR_VPS_IP` 🎉

---

## ⚡ วิธีที่ 2: Script อัตโนมัติ

### แค่ 2 คำสั่ง!

```bash
# 1. อัปโหลดไฟล์ (จากเครื่อง local)
scp -r * root@YOUR_VPS_IP:/root/spu-activity-hub/

# 2. รัน script
ssh root@YOUR_VPS_IP
cd /root/spu-activity-hub
chmod +x easy-deploy.sh
./easy-deploy.sh
```

**เสร็จแล้ว!** เปิด `http://YOUR_VPS_IP` 🎉

---

## 📋 เปรียบเทียบ

| วิธี | คำสั่ง | เวลา | ความยาก |
|------|--------|------|---------|
| **Docker** | 3 คำสั่ง | 5 นาที | ⭐ ง่ายมาก |
| **Script** | 2 คำสั่ง | 10 นาที | ⭐⭐ ง่าย |
| Manual | 30+ คำสั่ง | 30+ นาที | ⭐⭐⭐⭐ ยาก |

---

## 🎯 เลือกวิธีไหน?

- **ต้องการง่ายที่สุด** → ใช้ **Docker** 🐳
- **ไม่ต้องการ Docker** → ใช้ **Script** 📜
- **ต้องการควบคุมทุกอย่าง** → ดู `DEPLOY.md` 📖

---

## 🔧 คำสั่งที่มีประโยชน์

### Docker
```bash
docker compose ps      # ดูสถานะ
docker compose logs    # ดู logs
docker compose restart # รีสตาร์ท
docker compose down    # หยุด
```

### Script
```bash
pm2 status            # ดูสถานะ
pm2 logs              # ดู logs
pm2 restart all       # รีสตาร์ท
```

---

## 📚 เอกสารเพิ่มเติม

- `EASY_DEPLOY.md` - คู่มือแบบละเอียด
- `DEPLOY.md` - คู่มือแบบเต็ม (ถ้าต้องการทำเอง)
- `QUICK_DEPLOY.md` - คู่มือแบบเร็ว

---

## ⚠️ หมายเหตุ

- **Docker**: ต้องติดตั้ง Docker ก่อน (ใช้ script ที่ให้มา)
- **Script**: ทำงานบน Ubuntu/Debian
- **IP Address**: ใช้ IP ของ VPS แทน domain

**แนะนำ: ใช้ Docker!** 🐳

